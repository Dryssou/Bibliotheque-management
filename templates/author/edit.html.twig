{% extends 'base.html.twig' %}

{% block title %}Modifier {{ author.name }}{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    <div class="max-w-2xl mx-auto">
        <div class="bg-white shadow rounded-lg p-6">
            <div class="mb-6 flex justify-between items-center">
                <h1 class="text-2xl font-bold text-gray-900">Modifier {{ author.name }}</h1>
                <a href="{{ path('app_author_index') }}" class="text-indigo-600 hover:text-indigo-800">
                    Retour à la liste
                </a>
            </div>

            {{ include('author/_form.html.twig') }}

            <div class="mt-6 pt-6 border-t border-gray-200">
                {{ include('author/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}