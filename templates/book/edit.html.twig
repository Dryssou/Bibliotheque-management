{% extends 'base.html.twig' %}

{% block title %}Modifier {{ book.title }}{% endblock %}

{% block body %}
<div class="container mx-auto px-6 py-8">
    <div class="max-w-4xl mx-auto bg-white rounded-lg shadow-md overflow-hidden">
        <div class="px-6 py-4 border-b border-gray-200">
            <div class="flex items-center justify-between">
                <h1 class="text-3xl font-semibold text-gray-800">Modifier : {{ book.title }}</h1>
                <a href="{{ path('app_book_index') }}" class="text-indigo-500 hover:underline text-sm">
                    Retour à la liste des livres
                </a>
            </div>
        </div>

        <div class="px-6 py-6">
            <form>
                {{ include('book/_form.html.twig') }}
            </form>
        </div>

        <div class="px-6 py-4 bg-gray-50 border-t border-gray-200">
            <h2 class="text-lg font-medium text-gray-800 mb-4">Actions</h2>
            <div class="flex items-center space-x-4">
                <a href="{{ path('app_book_index') }}" class="px-4 py-2 text-sm font-medium text-gray-600 bg-gray-200 rounded-md hover:bg-gray-300">
                    Annuler
                </a>
                <div class="inline-block">
                    {{ include('book/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
